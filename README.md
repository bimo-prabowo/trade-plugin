# trade-plugin
A small MES/ES paper-trading dashboard based on Signal Deck Futures framework.  It runs a local server, pulls public Yahoo Finance chart/RSS data, computes MACD, RSI/VWAP, a volume-delta proxy, local pattern context, VIX/yield macro reads, and renders a live dashboard with an action of `LONG`, `SHORT`, or `WAIT`.
